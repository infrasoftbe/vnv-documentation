import { HomeIcon , UserIcon , CogIcon } from "@patternfly/react-icons";
import Playground from '@theme/Playground';
import BrowserOnly from "@docusaurus/BrowserOnly";

# @infrasoftbe/infrasoft-ui-components

<BrowserOnly>
{() => {

  const { ActionButton, ButtonContext } = require('@infrasoftbe/infrasoft-ui-components');

  return <Playground
    scope={{
      ActionButton,
      ButtonContext,
      HomeIcon,
      UserIcon,
      CogIcon
    }}
  >
  {`
    function Sample() {
      const buttonData = [
        {
          label: "Home",
          logo: HomeIcon,
          onClick: () => alert("Home clicked!"),
        },
        {
          label: "Profile",
          logo: UserIcon,
          onClick: () => alert("Profile clicked!"),
        },
        {
          label: "Settings",
          logo: CogIcon,
          onClick: () => alert("Settings clicked!"),
        },
      ];

      return (
        <div>
          <ActionButton buttonContext={buttonData} />
        </div>
      );
    }
  `}
  </Playground>

}}
</BrowserOnly>